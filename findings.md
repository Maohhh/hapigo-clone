# Findings

## 2026-04-18
- Task: fix Hapigo Clone Tauri DMG packaging and produce a distributable artifact.
- `package.json` exposes `npm run tauri` only; Tauri build command is `npm run tauri build`.
- `src-tauri/tauri.conf.json` is Tauri v1 style with bundle targets `["app", "dmg"]`.
- No project-owned DMG script exists; `src-tauri/target/release/bundle/dmg/bundle_dmg.sh` is generated by Tauri.
- Existing outputs include `Hapigo Clone.app`, `Hapigo Clone_1.0.0_aarch64.dmg`, and leftover `rw.Hapigo Clone_1.0.0_aarch64.dmg`.
- `src-tauri/icons/icon.png` is a 1x1 PNG and `icon` config is empty, so app icon quality is not the immediate DMG failure but should be cleaned up later.
